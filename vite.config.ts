
import { defineConfig } from 'vite';

export default defineConfig({
  // Reemplaza 'nombre-de-tu-repo' por el nombre de tu repositorio en GitHub
  base: './', 
  build: {
    outDir: 'dist',
  }
});
